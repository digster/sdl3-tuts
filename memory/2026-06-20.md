# 2026-06-20 — Add README + ARCHITECTURE for the docs site

## Goal
The repo had no `README.md` — anyone landing on `digster/sdl3-tuts` on GitHub had
no entry point. Add a root README, plus an `ARCHITECTURE.md` (per the user's global
convention to create one when absent).

## Decisions (confirmed with the user via AskUserQuestion)
- **Scope:** README **and** ARCHITECTURE.md (not README only).
- **Header style:** clean Markdown, **no shields.io badges** — match the site's
  understated tone.

## What was added
- `README.md` — what the site is (static HTML, 61 recipes); a tracks table
  (2D=20 / 3D=21 / bgfx=20, verified via `ls`); viewing instructions
  (`python3 -m http.server`, optional GitHub Pages URL); running instructions with
  a file→`make` mapping; the four `SDL_App*` callbacks + heap `AppState` model;
  recipe-page anatomy; repo layout tree; tech stack (Prism `prism → c → cpp`);
  MIT license.
- `ARCHITECTURE.md` — zero-build static sub-site; the **parent `sdl3-2d` template
  relationship** (build harness `examples/` + `Makefile` lives one level up, which
  is why `../README.md` / `../../examples/...` links dangle by design — table of
  expected-outward vs must-resolve links); page topology (root → track → recipe,
  footer Next chains + cross-track terminator); contributor conventions
  (append-don't-renumber, self-contained listings, Prism load order, HTML-escaping
  code blocks, keeping counts in sync, badges); the `memory/` + `PROMPT.md`
  workflow; the structural verification routine.

## Key facts captured
- Counts verified: `2d=20`, `3d=21`, `bgfx=20` recipe HTML files; **65 total HTML
  pages** (61 recipes + 3 track indexes + 1 root).
- The honest framing throughout: this repo is the **tutorial half**; the runnable
  half (examples + Makefile) is a *separate* parent template, not included here.

## Files modified
- `README.md` (new), `ARCHITECTURE.md` (new).
- `PROMPT.md` — 2026-06-20 entry.
- `memory/2026-06-20.md` — this file.

## Not done (intentional / out of scope)
- Did not fix the dangling `../README.md` / `../../examples/...` links in recipe
  pages or the known Prism `prism-c` ordering bug on older 2D/3D pages.
- No commit (repo convention: generate the message, user commits).
